 一、期中范围与高频考点总览

从课件和作业的对应关系看，期中复习主线基本就是下面 8 个专题：

1. 操作系统基础抽象
2. 异常控制流 ECF：interrupt / trap / fault / abort
3. 进程：状态、`fork`、`waitpid`、`execve`
4. 信号：发送、接收、阻塞、处理器、竞争条件
5. CPU 调度：FCFS、SJF、STCF、RR、MLFQ、比例分享
6. 虚拟内存：地址空间、页、页表、缺页、工作集、抖动
7. 地址翻译：segmentation、paging、TLB、多级页表、cache
8. 动态内存分配：`malloc/free/realloc`、碎片、空闲链表、常见内存错误

从作业映射看：

- 作业 1 对应异常、系统调用
- 作业 2 对应进程与 `fork`
- 作业 3 对应信号与调度
- 作业 4 对应动态内存与调试工具
- 作业 5 对应地址翻译、TLB、页表、分段

如果老师按“课件 + 作业”出题，最可能的题型是：

- 概念辨析题：几种异常、用户态/内核态、进程状态、zombie、pending/block 等
- 程序输出题：`fork`、`waitpid`、signal handler、并发输出交错
- 计算题：CPU 利用率、响应时间、周转时间、VA/PA 分解、TLB/cache 命中判断、多级页表层数
- 简答题：为什么要分离内核栈和用户栈、为什么需要虚拟内存、TLB 为什么能加速、`gdb` 与 `valgrind` 的区别
- 代码题：系统调用汇编、`snooze`、信号处理、简单 `malloc/realloc` 使用

---

## 二、按知识点梳理

## 1. 操作系统基础抽象

操作系统是硬件和应用之间的中间层，核心作用有两点：

- 管理和封装硬件资源
- 给程序提供抽象，让程序更容易写，也更安全

课件里反复强调的 3 个核心抽象：

- 进程：对 CPU/计算资源的抽象
- 虚拟内存：对主存资源的抽象
- 文件：对持久化 I/O 资源的抽象

还需要掌握：

- 应用程序不能直接安全地访问硬件，必须通过 OS 提供的接口
- OS 不只是“一个程序”，而是一组管理硬件的程序
- 用户态和内核态的区分，是现代 OS 保护机制的基础
- 系统调用本质上是“受控地进入内核”

可能考点：

- OS 为什么需要“抽象”而不是直接暴露硬件
- 单内核、层次式、微内核的基本区别
- 为什么说进程给程序制造了“独占机器”的错觉

---

## 2. 异常控制流 ECF

### 2.1 什么是 ECF

正常控制流是程序按指令顺序执行、调用函数、返回。

异常控制流 ECF 指控制流因为“特殊事件”突然改变，例如：

- I/O 中断
- 系统调用
- 缺页
- 非法指令
- 进程切换
- 信号处理

ECF 是“进程、虚拟内存、信号、上下文切换”等机制的基础。

### 2.2 四类异常一定要分清

#### interrupt

- 异步异常
- 来自处理器外部设备
- 当前指令执行完后再处理
- 处理结束后通常返回下一条指令

典型例子：

- 磁盘读完一个块
- 网卡收到数据包
- 时钟中断

#### trap

- 同步异常
- 由当前指令故意触发
- 常见用途是系统调用
- 处理结束后通常返回下一条指令

典型例子：

- `read()`
- `write()`
- `kill()`
- `syscall`

#### fault

- 同步异常
- 由当前指令触发，但“可能可修复”
- 若修复成功，通常重新执行当前指令
- 若修复失败，常转化为信号或终止

典型例子：

- page fault
- 访问非法地址引发保护异常

#### abort

- 同步异常
- 严重、不可恢复错误
- 通常不能恢复

典型例子：

- 硬件故障
- 机器校验错误

### 2.3 trap 和普通函数调用的区别

系统调用看起来像函数调用，但本质不同：

- 普通函数调用只切换控制流，不切换特权级
- 系统调用会从用户态进入内核态
- 系统调用由 OS 根据 syscall number 分发到对应处理程序

### 2.4 page fault 的两种后果

这点很容易考：

- 页合法，但当前不在内存中：fault handler 把页调入内存，然后重新执行指令
- 地址本身非法：无法修复，通常向进程发送 `SIGSEGV`

### 2.5 用户态和内核态

要点：

- 应用初始运行在用户态
- 发生异常后进入内核态，由内核处理
- 返回用户程序时再回到用户态
- 内核态拥有更多特权，能执行特权指令、访问受保护资源

### 2.6 上下文切换

内核维护每个进程的上下文，包括：

- 寄存器状态
- 程序计数器
- 栈信息
- 虚拟内存状态
- 打开的文件等

进程切换发生时：

1. 保存当前进程上下文
2. 选中下一个进程
3. 恢复其上下文
4. 跳回其控制流

### 2.7 高频易错点

- 中断是异步，trap/fault/abort 一般是同步
- page fault 不等于“程序一定错了”，合法页不在内存里也会 page fault
- 系统调用是 trap，不是 interrupt
- “收到一封邮件”这种外设事件属于 interrupt
- “PC 内存损坏”属于 abort

---

## 3. 进程

### 3.1 进程的定义

进程是一个正在运行的程序实例。

每个进程有：

- 独立的逻辑控制流
- 私有地址空间
- 内核维护的上下文

### 3.2 进程状态

课件中的状态一定要会画图、会解释：

- New：新建
- Ready：就绪
- Running：运行
- Blocked：阻塞
- Stopped：停止
- Terminated：终止

Linux 常见状态：

- `R`：运行或就绪
- `S`：可中断睡眠
- `D`：不可中断睡眠
- `T`：停止
- `Z`：僵尸

易错点：

- 时间片用完一般是 Running -> Ready，不是 Blocked
- 等待 I/O、`sleep`、等锁都属于阻塞
- 被 `SIGSTOP` 停止不是终止

### 3.3 `fork`

`fork()` 是创建子进程的核心系统调用。

最重要的性质：

- 调用一次，返回两次
- 父进程返回子进程 PID
- 子进程返回 0
- 父子进程拥有“相同但彼此独立”的地址空间副本
- 打开的文件是共享的，所以 `stdout` 等输出会互相影响
- 两个进程之后的执行次序不可预测

必须背住的结论：

- 每执行一次 `fork`，当前每个活着的进程都会复制出一个新进程
- 连续 `n` 次 `fork`，若没有提前退出，总进程数是 `2^n`
- 若每个进程都执行一条 `printf("hello\n")`，总输出行数就是 `2^n`

### 3.4 `fork` 后哪些共享，哪些不共享

不共享：

- 变量值的后续修改
- 地址空间中的数据内容

共享或“看起来共享”的：

- 打开的文件描述符对应的内核对象
- 文件偏移、终端等外部资源

所以课上特别容易出：

- 父子进程局部变量各自修改，输出不同
- 父子进程对同一个 `stdout` 交错打印

### 3.5 zombie 与回收

子进程终止后，内核不会立即彻底清除它，而是保留退出状态等信息，直到父进程回收。

这时子进程是 zombie。

要点：

- zombie 不再运行
- 但仍占有 PID 和少量内核表项
- 父进程应使用 `wait()` / `waitpid()` 回收
- 若父进程先退出，孤儿 zombie 会由 `init`/类似机制接管并回收

### 3.6 `waitpid`

高频考点：

- `waitpid(pid, &status, 0)`：阻塞等待指定子进程
- `waitpid(-1, &status, 0)`：等待任意一个子进程
- `WNOHANG`：不阻塞
- `WUNTRACED`：子进程停止也返回
- `WCONTINUED`：子进程继续运行也返回

状态解析宏：

- `WIFEXITED(status)`：是否正常退出
- `WEXITSTATUS(status)`：退出码
- `WIFSIGNALED(status)`：是否因未捕获信号终止
- `WTERMSIG(status)`：终止信号编号
- `WIFSTOPPED(status)`：是否停止
- `WSTOPSIG(status)`：导致停止的信号
- `WIFCONTINUED(status)`：是否被 `SIGCONT` 恢复

### 3.7 `sleep` / `pause`

- `sleep(secs)`：休眠指定秒数
- 若中途被信号打断，会提前返回剩余未睡秒数
- `pause()`：一直挂起直到收到一个信号

所以 `snooze` 包装函数的经典写法是：

```c
unsigned int snooze(unsigned int secs) {
    unsigned int rc = sleep(secs);
    printf("Slept for %u of %u secs.\n", secs - rc, secs);
    return rc;
}
```

### 3.8 `execve`

`execve(filename, argv, envp)` 的本质：

- 不创建新进程
- 而是“用一个新程序替换当前进程的用户空间内容”
- 只有失败时才返回

`fork + execve` 组合是 shell 和服务器创建新程序的基础。

### 3.9 shell 模型

简单 shell 的逻辑：

1. 读命令行
2. 解析参数
3. 若是内建命令则直接执行
4. 否则 `fork()`
5. 子进程 `execve()`
6. 父进程根据前台/后台决定是否 `waitpid()`

这部分很适合考简答或流程题。

---

## 4. 信号

### 4.1 信号是什么

信号是 Linux/Unix 进程间通信的一种轻量机制，是软件层面的异常通知。

两步过程：

1. 发送 signal
2. 目标进程接收并处理 signal

### 4.2 常见信号

- `SIGINT`：通常来自 `Ctrl+C`
- `SIGTSTP`：通常来自 `Ctrl+Z`
- `SIGKILL`：强制终止，不能捕获或忽略
- `SIGSTOP`：停止进程，不能捕获或忽略
- `SIGCONT`：继续运行
- `SIGCHLD`：子进程状态变化
- `SIGALRM`：闹钟时间到
- `SIGSEGV`：非法内存访问
- `SIGFPE`：算术异常，如除零
- `SIGILL`：非法指令

### 4.3 发送和接收

发送的来源可以是：

- 用户输入
- 内核检测到异常事件
- 一个进程调用 `kill()` 给另一个进程发信号

接收的处理方式有 4 种：

- 忽略
- 终止
- 停止
- 捕获并执行 handler

### 4.4 pending 与 blocked

必须掌握：

- pending：已经发送但还未被接收
- blocked：该进程当前屏蔽了某类信号

经典结论：

- 标准信号通常不排队
- 某时刻同一类型至多记一个 pending 位
- 被 block 的信号可以先变成 pending，等解除屏蔽后再处理

### 4.5 进程组与键盘信号

shell 会把一个 job 放进同一个 process group。

因此：

- `Ctrl+C` 通常发给前台进程组
- `Ctrl+Z` 也发给前台进程组
- `kill` 可以向单个进程发，也可以向进程组发

### 4.6 `signal` 与 `sigaction`

`signal()` 用来安装 handler，但历史行为和可移植性不如 `sigaction()`。

考试层面要知道：

- `sigaction()` 更标准、更强大
- 可以控制屏蔽集和行为
- 更适合严谨的信号处理

### 4.7 handler 编写规则

这块老师很容易考“哪里写错了”：

- handler 要尽量短
- 只做必要事情
- 只调用 async-signal-safe 函数
- 不要在 handler 里随便用 `printf`、`malloc` 等不安全函数
- 进入 handler 前后要注意保存与恢复 `errno`
- handler 与主程序并发，访问全局变量要谨慎

### 4.8 用信号同步，避免竞争条件

典型 bug：

- 父进程刚 `fork`，还没把子进程加入作业表
- 子进程很快退出
- `SIGCHLD` handler 先执行，把子进程回收掉
- 主程序再去登记时，状态已经不一致

常见修复思路：

1. 在关键区前先 `sigprocmask(SIG_BLOCK, ...)` 屏蔽 `SIGCHLD`
2. 完成共享数据结构更新
3. 再解除屏蔽

如果显式等待信号而不是忙等，常用：

- `sigsuspend()`

它适合原子地“临时替换 mask 并睡眠等待信号”。

### 4.9 高频易错点

- `kill` 不等于“杀进程”，它是“发送信号”
- `SIGKILL` 和 `SIGSTOP` 不能被捕获、阻塞或忽略
- 标准信号不排队
- handler 和 main 并发，不是“串行执行的普通函数”

---

## 5. CPU 调度

### 5.1 两个核心指标

#### turnaround time 周转时间

完成时刻 - 到达时刻

#### response time 响应时间

第一次开始运行时刻 - 到达时刻

一定要区分：

- 周转时间看的是“最终完成得快不快”
- 响应时间看的是“第一次被响应得快不快”

### 5.2 FCFS / FIFO

先来先服务，按到达顺序运行。

优点：

- 简单

缺点：

- convoy effect
- 长任务在前时会严重拖慢平均周转时间

### 5.3 SJF

Shortest Job First，选择最短作业先执行。

结论：

- 若所有任务同时到达，SJF 对平均周转时间最优

局限：

- 需要预知运行时间
- 非抢占式时，若长任务先开始，仍可能不理想

### 5.4 STCF

Shortest Time-to-Completion First，也可理解为抢占式 SJF / SRTF。

思想：

- 总是运行“剩余时间最短”的任务

比 SJF 更灵活，平均周转时间通常更优。

### 5.5 RR

Round Robin，时间片轮转。

特点：

- 响应时间好
- 公平
- 时间片过小会导致上下文切换开销大
- 周转时间往往不如 SJF / STCF

### 5.6 MLFQ

多级反馈队列是课上重点。

核心规则可概括为：

- 高优先级先运行
- 同优先级用 RR
- 新任务先进高优先级队列
- 用完配额后降级
- 经过一段时间整体优先级提升

它希望同时兼顾：

- 交互型任务的低响应时间
- CPU-bound 任务的吞吐

要会说明它解决了什么问题、又带来了什么问题：

- 能动态“猜测”任务类型
- 可能饥饿
- 可能被恶意 I/O 行为欺骗
- 需要 priority boost 和更精细 accounting

### 5.7 比例分享

核心思想：

- 不再追求平均周转或响应最优
- 而是保证任务获得一定比例的 CPU

代表：

- Lottery scheduling
- Stride scheduling

### 5.8 多核调度

多核下新问题：

- cache affinity
- load imbalance
- 任务迁移成本
- 共享数据竞争

两个典型思路：

- SQMS：单队列多核调度
- MQMS：每核一个队列

没有一种策略在所有场景都绝对最好。

---

## 6. 虚拟内存 VM

### 6.1 为什么需要虚拟内存

课件中强调 3 个目的：

- 高效使用 DRAM，让主存像磁盘上更大地址空间的 cache
- 简化内存管理
- 提供保护与共享

### 6.2 地址空间

一个进程看到的是自己的虚拟地址空间，典型区域包括：

- code / text
- data / bss
- heap
- stack
- 共享库映射区
- 内核虚拟空间

程序员看到和打印出来的地址基本都是虚拟地址，不是物理地址。

### 6.3 页与页表

基本单位：

- 虚拟页 VP
- 物理页 / 页框 PP

页表作用：

- 把 VPN 映射到 PPN
- 记录页是否有效、是否在内存、读写执行权限等

### 6.4 page hit / page fault

- 访问页已在内存：page hit
- 访问页不在内存：page fault

缺页处理流程：

1. CPU 产生缺页异常
2. 内核进入 page fault handler
3. 选择牺牲页
4. 从磁盘把目标页调入内存
5. 更新页表
6. 重新执行原指令

### 6.5 局部性与工作集

虚拟内存有效的前提是局部性：

- 时间局部性
- 空间局部性

工作集概念：

- 某段时间内频繁访问的页集合

如果所有进程工作集总和超过物理内存，就可能发生：

- Thrashing（抖动）

即系统大量时间花在换页上，性能崩溃。

### 6.6 VM 带来的额外价值

#### 简化分配

虚拟上连续，不要求物理上连续。

#### 简化装载

`execve()` 可以先建立无效 PTE，按需调页装入。

#### 支持共享

不同进程的虚拟页可以映射到同一物理页。

#### 支持保护

不同进程对同一物理页可以有不同权限。

---

## 7. 地址翻译：分段、分页、TLB、多级页表

### 7.1 base and bounds

最早的办法是：

- 一个 base
- 一个 bounds

物理地址 = base + offset

越界则异常。

问题：

- 整个地址空间必须连续放置
- heap 和 stack 中间未使用空间也会浪费物理内存

### 7.2 segmentation

分段思想：

- 不再把整个进程当一整块
- code、data、heap、stack 分开映射

优点：

- 比 base-bounds 更灵活

缺点：

- 外部碎片
- 压缩回收开销大
- 稀疏地址空间内部仍可能浪费

关于 stack 段要特别注意：

- stack 常常是负增长的
- 所以地址换算不能简单“base + offset”
- 需要根据 positive 标记判断是正增长还是负增长

### 7.3 paging

分页把地址空间切成固定大小页面。

优点：

- 没有分段那种外部碎片问题
- 分配和回收简单
- 更适合稀疏空间

缺点：

- 页表可能很大
- 地址翻译需要额外访存

### 7.4 地址拆分公式

若页大小为 `2^p`：

- VPO 位数 = `p`
- VPN 位数 = `n - p`
- PPO 位数 = `p`
- PPN 位数 = `m - p`

其中：

- `n` 是虚拟地址位数
- `m` 是物理地址位数

### 7.5 TLB

TLB 是 MMU 中的地址翻译缓存。

用途：

- 缓存页表项
- 避免每次地址翻译都先访问页表

典型考法：

- 给定 VA，拆出 `VPN`、`VPO`
- 再将 `VPN` 分成 `TLBT` 和 `TLBI`
- 查 TLB 判断 hit/miss
- hit 时得到 `PPN`
- 再拼出 `PA`

### 7.6 TLB 与进程切换

TLB 中缓存的是当前进程的地址翻译结果。

进程切换时要考虑：

- 直接清空 TLB
- 或使用 ASID 等机制区分不同进程

### 7.7 Cache 与地址翻译

现代 cache 通常按物理地址访问。

因此通常流程是：

1. 先做 VA -> PA 翻译
2. 再用 PA 访问 cache

所以一道完整大题可能会连着考：

- VA 拆分
- 页表 / TLB
- 得到 PA
- 再做 cache tag/index/offset 分解

### 7.8 多级页表

线性页表的问题：

- 每个 VPN 都要一个表项
- 即使大量地址没用到，也得占空间

多级页表的思想：

- 把 VPN 再拆成多级索引
- 只为实际用到的部分分配下级页表

计算层数常用方法：

1. 先算页内偏移位数
2. 得到 VPN 总位数
3. 计算每级页表能提供多少索引位
4. 用 `ceil(VPN位数 / 每级索引位数)` 得层数

### 7.9 反向页表

反向页表以物理页为中心，而不是虚拟页为中心。

优点：

- 物理页是稠密的，表规模更可控

缺点：

- 查找和管理更复杂

---

## 8. 动态内存分配与内存错误

### 8.1 堆与 `malloc/free/realloc`

- `malloc(size)`：分配一块未初始化堆内存
- `calloc(n, size)`：分配并清零
- `realloc(ptr, newsize)`：调整已分配块大小
- `free(ptr)`：归还堆块

注意：

- `malloc/free` 不是系统调用，是库函数
- 库函数底层可能通过 `brk` 等机制向 OS 申请更多堆空间

### 8.2 分配器的两个目标

- Throughput：分配/释放速度
- Memory utilization：内存利用率

### 8.3 碎片

#### 内部碎片

- 块已经分配给你了
- 但实际 payload 比块小

#### 外部碎片

- 空闲总量够
- 但没有足够大的连续空闲块

### 8.4 隐式空闲链表

特点：

- 所有块按地址顺序串起来
- 通过 header 中的 size/alloc 信息找到下一块

放置策略：

- first fit
- next fit
- best fit

释放后常做 coalescing 合并相邻空闲块。

### 8.5 boundary tag

为支持向前合并，可在块尾部再存一个 footer。

优点：

- 常数时间判断前一块大小和状态

缺点：

- 额外空间开销

### 8.6 显式空闲链表

只把 free block 连成链表，不遍历已分配块。

优点：

- 分配效率比隐式链表高

缺点：

- free block 需要额外指针域

### 8.7 segregated list 与 buddy system

segregated list：

- 按大小类别维护多个空闲链表

buddy system：

- 以 2 的幂组织块
- 易于分裂与合并
- 但内部碎片可能较明显

### 8.8 垃圾回收

课程里主要是理解概念：

- 显式内存管理：程序员自己 `free`
- 隐式内存管理：GC 自动回收

常见 GC 思想：

- 引用计数
- 标记清除
- 复制回收
- 分代回收

### 8.9 常见内存错误

这个部分几乎和作业 4 一一对应，必须会认：

#### 空指针解引用

- 典型结果：崩溃、`SIGSEGV`

#### 越界访问

- 如 `data[100]` 写到 100 个元素数组末尾之外
- 结果是未定义行为

#### use-after-free

- 释放后继续访问

#### invalid free

- 对不是原始 `malloc` 返回值的地址调用 `free`

#### memory leak

- 分配了但没有释放

### 8.10 `gdb` 和 `valgrind`

一定要会比较：

#### `gdb`

- 擅长定位崩溃点
- 看调用栈、变量值、断点单步
- 对“没崩但错了”的问题不一定敏感

#### `valgrind`

- 擅长发现内存错误
- 如 invalid read/write、use-after-free、leak
- 即使程序没立刻崩，也能报告

一句话总结：

- `gdb` 更像“定位崩在哪”
- `valgrind` 更像“检查内存哪里已经坏了”

---

## 三、必须会背的公式、结论与易错点

### 1. CPU 利用率

若每个进程有概率 `p` 处于 I/O 等待，内存中有 `n` 个并发进程，则：

- CPU 空闲概率 = `p^n`
- CPU 利用率 = `1 - p^n`

### 2. 多次 `fork`

- 总进程数：`2^n`
- 若每个进程打印一行，则总输出行数也是 `2^n`

### 3. 页大小与位数关系

若页大小 = `2^p` 字节：

- 页内偏移位数 = `p`

### 4. 多级页表层数

- 每级索引位数 = `log2(每页可容纳的 PTE 数)`
- 总层数 = `ceil(VPN总位数 / 每级索引位数)`

### 5. 栈段负增长

遇到 segmentation 题不要忘：

- stack 可能是负增长
- 此时物理地址换算不是简单 `base + offset`

### 6. 信号 handler

考试里常见错法：

- 在 handler 里 `printf`
- 在 handler 里做复杂逻辑
- 忘了保存恢复 `errno`
- 忽视 handler 与主程序的并发关系

### 7. `kill`

- `kill` 是“发信号”，不是“必定杀死”
- `kill -9` 才是发送 `SIGKILL`

### 8. `waitpid`

- 回收顺序不一定和创建顺序一致
- `waitpid(-1, ...)` 回收任意子进程时，返回顺序是 nondeterministic

---

## 四、课件与作业练习题汇总

这一部分把题目按专题重新归并，便于刷题。

## A. 异常、系统调用、用户态/内核态

### 题 1：判断异常类型

来源：

- 作业 1 第 1 题
- `2-2-exception.pptx` 课堂练习

题目类型：

- page fault
- 收到一封来自 Internet 的邮件
- shell 中执行 `kill -9 <pid>`
- PC 内存损坏
- 程序访问数组越界
- 磁盘读一个数据块完成
- 调用 `read()`
- 访问的页不在物理内存

标准答案要点：

- page fault：`Fault`，同步
- 收到邮件/磁盘 I/O 完成：`Interrupt`，异步
- 执行 `read()` / 系统调用 / `kill` 命令触发系统调用：`Trap`，同步
- PC 内存损坏：`Abort`
- 访问页不在物理内存：`Fault`，同步
- 数组越界若导致非法地址访问，通常按 `Fault`，同步处理

### 题 2：写系统调用汇编

来源：

- 作业 1 第 2 题

题意：

- 直接使用 `syscall`
- `kill` 的系统调用号按题目给定为 `37`
- 写出 `kill(2024, 3); exit(0);`

参考答案：

```asm
.globl main
main:
    movq    $37, %rax
    movq    $2024, %rdi
    movq    $3, %rsi
    syscall

    movq    $60, %rax
    xorq    %rdi, %rdi
    syscall
```

考点：

- x86-64 系统调用参数寄存器
- syscall number 放在 `%rax`
- 前几个参数依次在 `%rdi`, `%rsi`, `%rdx`, `%r10`, `%r8`, `%r9`

### 题 3：为什么内核栈和用户栈要分离

来源：

- `2-2-exception.pptx` 课堂练习

答题要点：

- 内核不能信任用户栈，用户程序可能破坏它
- 内核执行异常处理和系统调用时需要安全、受保护的栈空间
- 分离后便于特权隔离，避免用户态数据污染内核执行
- 也便于为每个进程单独保存内核态上下文

### 题 4：CPU 利用率计算

来源：

- `2-2-exception.pptx` 课堂练习

题目 1：

一个系统内存能同时容纳 5 个程序，每个程序有一半时间在等 I/O。CPU 有多少时间被浪费？

答案：

- 空闲率 = `0.5^5 = 1/32 = 3.125%`
- 利用率 = `96.875%`

题目 2：

4GB RAM，其中内核占 512MB；每个进程 256MB；目标 CPU 利用率 99%，最大可容忍 I/O wait 比例是多少？

答案：

- 可并发进程数 `n = (4096 - 512) / 256 = 14`
- `1 - p^14 = 0.99`
- `p^14 = 0.01`
- `p ≈ 0.7197`

结论：

- 最大 I/O 等待比例约 `71.97%`

---

## B. 进程、`fork`、`waitpid`

### 题 1：连续 `fork` 的输出行数

来源：

- 作业 2 第 1 题

结论：

- `n` 次 `fork` 后总进程数 `2^n`
- 每个进程打印一行，则输出 `2^n` 行

### 题 2：并发输出顺序判断

来源：

- 作业 2 第 2 题

结论：

- 已知子进程先输出 `a` 再退出
- 父进程输出 `b`，`waitpid` 后输出 `c`
- 因此可能输出只有：
  - `abc`
  - `bac`

关键点：

- `c` 一定最后
- `a` 和 `b` 先后不确定

### 题 3：父子进程变量分离

来源：

- 作业 2 第 3 题

结论：

- 父进程输出 `x=4`、`x=3`
- 子进程输出 `x=2`
- 所有可能顺序：
  - `x=2 x=4 x=3`
  - `x=4 x=2 x=3`
  - `x=4 x=3 x=2`

考点：

- `fork` 后变量副本独立
- 父子进程执行顺序不可预测

### 题 4：四个进程拼接输出

来源：

- 作业 2 第 4 题

结论：

- 4 个进程最终各输出：`1`、`12`、`0`、`02`
- 可能答案：A、C、E

这类题做法：

1. 先画进程树
2. 确定每个进程本地会打印什么
3. 再判断哪些交错顺序可能出现

### 题 5：阻塞原因判断

来源：

- `2-3-process.pptx` 课堂练习

题目：

- 进程阻塞的原因不包括什么

答案：

- `A. 时间片切换`

因为：

- 时间片切换是 Running -> Ready
- 不是 Running -> Blocked

### 题 6：带 `waitpid` 的输出题

来源：

- `2-3-process.pptx` 课堂练习

程序关键行为：

- 先打印 `Hello`
- `fork` 后父子都打印 `!pid`
- 子进程打印 `1`、`Bye`，并以 `2` 退出
- 父进程打印 `0`，`waitpid` 收到子进程退出码后打印 `2`，再打印 `Bye`

结论：

- 总输出行数是 6 行
- 一个可能顺序：

```text
Hello
0
1
Bye
2
Bye
```

也可能是：

```text
Hello
1
0
Bye
2
Bye
```

核心约束：

- `Hello` 一定最先
- 父进程打印退出码 `2` 一定在子进程退出之后
- 最后的 `Bye` 是父进程打印的

### 题 7：`snooze`

来源：

- `2-3-process.pptx` 课堂练习
- 作业 3 第 2 题

标准实现：

```c
unsigned int snooze(unsigned int secs) {
    unsigned int rc = sleep(secs);
    printf("Slept for %u of %u secs.\n", secs - rc, secs);
    return rc;
}
```

若睡 5 秒，在第 3 秒被 `Ctrl+C` 打断，则输出：

```text
Slept for 3 of 5 secs.
```

### 题 8：数组分块打印

来源：

- `2-3-process.pptx` 课堂练习

思路：

- 每次 `fork` 后把 `[start, start+len)` 对半分
- 父子各拿一半
- 连续两次分裂可得到 4 个进程，各打印 1/4

若要求有序输出，还要加：

- `waitpid`
- 或进一步同步机制

---

## C. 信号

### 题 1：带 handler 的输出

来源：

- 作业 3 第 1 题
- `2-4-signal.pptx` 课堂练习

结论：

- 输出是 `213`

理解：

- 父进程先打印初值 `2`
- 子进程收到 `SIGUSR1` 后，handler 把 `counter` 改成 `1` 并打印
- 父进程 `waitpid` 后把自己的 `counter` 变成 `3` 再打印

关键考点：

- 父子进程变量彼此独立
- handler 改的是“各自进程”的副本

### 题 2：素数程序 + `SIGINT`

来源：

- `2-4-signal.pptx` 课堂练习

题意：

- 程序持续搜索素数
- 收到 `SIGINT` 时打印当前找到的最大素数

考点：

- `signal(SIGINT, handler)`
- handler 里做最小工作
- 主循环与信号处理并发

### 题 3：指出错误 handler 的问题

来源：

- `2-4-signal.pptx` 课堂练习

典型问题包括：

- 在 handler 中使用 `printf`
- 在 handler 中调用复杂/不安全函数
- 未保存和恢复 `errno`
- 逻辑过重，耗时过长

更安全的思路：

- 保存 `olderrno`
- 用 `write` 或教材中的 `sio_*`
- 尽量短小
- 做完后恢复 `errno`

### 题 4：信号继承输出题

来源：

- `2-4-signal.pptx` 课堂练习

答案：

- 输出 `213`

这题本质仍然是在考：

- `fork` 后子进程继承父进程的 signal disposition
- 父子各自执行流和变量副本

---

## D. 调度

### 题 1：FCFS / RR / 抢占式 SJF / MLFQ

来源：

- 作业 3 第 3 题

任务：

```text
1:(0,8)  2:(0,3)  3:(0,2)  4:(2,10)  5:(2,1)
```

这里 `(到达时间, 运行时间)`。

结论：

- FCFS：平均周转时间 `15`，平均响应时间 `10.2`
- RR（时间片 1）：平均周转时间 `13`，平均响应时间 `1.2`
- 抢占式 SJF：平均周转时间 `9`，平均响应时间 `4.2`
- 本题给定参数下 MLFQ 与 RR 结果相同：平均周转时间 `13`，平均响应时间 `1.2`

这题必须会自己重算。

### 题 2：5 个批处理作业调度

来源：

- `2-5-schedule.pptx` 课堂练习

题目：

- 作业 A-E 几乎同时到达
- 运行时间分别为 `10, 6, 2, 4, 8`
- 优先级分别为 `3, 5, 2, 1, 4`，其中 `5` 最高
- 计算轮转法、优先级调度、先来先服务、最短作业优先的平均周转时间

已知可直接算出的结果：

- FCFS：`(10 + 16 + 18 + 22 + 30) / 5 = 19.2`
- SJF：顺序 `C, D, B, E, A`，平均周转时间 `14`
- 非抢占优先级：顺序 `B, E, A, C, D`，平均周转时间 `20.0`

说明：

- 轮转法需要知道时间片参数；PPT 文本抽取未带出该参数，复习时建议按老师课堂设定再手算一遍

### 题 3：调度算法比较

这类简答题很可能考：

- 哪个算法周转时间更优
- 哪个算法响应时间更优
- 为什么 RR 响应快但总周转未必最优
- 为什么 MLFQ 适合交互式任务

建议背诵：

- FCFS 简单但容易 convoy
- SJF / STCF 对平均周转时间友好
- RR 对响应时间友好
- MLFQ 兼顾交互和吞吐，但规则复杂

---

## E. 虚拟内存、地址翻译、TLB、页表

### 题 1：地址位数与容量换算

来源：

- `2-6-vm.pptx` 课堂练习

高频结论：

- 8 位地址空间大小 = `2^8 = 256B`
- 16 位地址空间大小 = `64KB`
- 32 位地址空间大小 = `4GB`
- 48 位地址空间大小 = `256TB`
- 64 位地址空间大小 = `16EB`

### 题 2：分段地址翻译

来源：

- `2-7-addr-trans.pptx` 课堂练习
- 作业 5 第 3 题

必须掌握的做法：

1. 根据虚拟空间大小求 VA 位数
2. 依据最高几位判断 segment
3. 求 offset
4. 根据 `positive` 判断正增长还是负增长
5. 检查越界
6. 计算 PA

作业 5 第 3 题标准结论：

- 局部对象 `s` 在 `stack`
- `s = 0xC1300` 对应 `PA = 0xFED00`
- `VA = 258K` 对应 `PA = 0x200800`
- `VA = 514K` 对应 `PA = 0x700800`

### 题 3：简单分页地址翻译

来源：

- `2-7-addr-trans.pptx` 课堂练习

对 `VA = 0x03D4`：

- `VPN = 0x0F`
- `VPO = 0x14`
- 无 page fault
- `PPN = 0x0D`
- `PA = 0x354`

### 题 4：页大小变化时位数怎么分

来源：

- `2-7-addr-trans.pptx` 课堂练习

已知：

- VA = 32 位
- PA = 24 位

答案：

- `P=1KB`：`VPN=22, VPO=10, PPN=14, PPO=10`
- `P=2KB`：`VPN=21, VPO=11, PPN=13, PPO=11`
- `P=4KB`：`VPN=20, VPO=12, PPN=12, PPO=12`
- `P=8KB`：`VPN=19, VPO=13, PPN=11, PPO=13`

### 题 5：TLB + Cache 联合题

来源：

- `2-7-addr-trans.pptx` 课堂练习
- 作业 5 第 1 题

作业 5 第 1 题给定 `VA = 0x03FC`，结论：

- `VPN = 0x0F`
- `TLBI = 3`
- `TLBT = 0x03`
- `TLB hit = Y`
- `PPN = 0x0D`
- `PA = 0x37C`
- `Cache index = 0xF`
- `Cache tag = 0x0D`
- `Cache hit = N`

课件中的类似题 `VA = 0x03D4`：

- `VPN = 0x0F`
- `TLBI = 3`
- `TLBT = 0x03`
- `TLB hit = Yes`
- `Page Fault = No`
- `PPN = 0x0D`
- `PPO = 0x14`
- `PA = 0x354`

继续做 cache：

- `CO = 0x0`
- `CI = 0x05`
- `CT = 0x0D`

### 题 6：多级页表层数

来源：

- 作业 5 第 2 题

已知：

- 虚拟地址空间 64 位
- page size = 2048B = `2^11`
- PTE 大小 4B
- 页表自身也按 page 分配

做法：

- 页内偏移 `11` 位
- VPN 位数 `64 - 11 = 53`
- 每级能容纳 `2048 / 4 = 512 = 2^9` 个表项
- 每级可提供 `9` 位索引
- 层数 `ceil(53 / 9) = 6`

结论：

- 需要 `6` 级页表

### 题 7：两级页表权限判断

来源：

- 作业 5 第 4 题

结果要记住题型，不必死背数值，但要会方法：

- 切分 VA：PDE index、PTE index、offset
- 找 PDE 地址
- 找 PTE 地址
- 检查 `P/W/U` 权限位
- 权限合法才可得到最终成功访问

作业答案中的结论：

- Process 1 写 `0xC1B2`：成功，`PA = 0x3A72`
- Process 2 写 `0x728F`：页不可写，虽然地址翻译到 `0x27CF`，但最终结果是 `page not writable`

---

## F. 动态内存、调试工具、实验类题目

### 题 1：空指针解引用

来源：

- 作业 4 第 1-3 题

结论：

- 直接运行通常崩溃
- `gdb` 可定位到触发 `SIGSEGV` 的源码行
- `valgrind` 会报 `invalid read/write`

### 题 2：只 `malloc` 不 `free`

来源：

- 作业 4 第 4 题

结论：

- 程序可能正常退出
- 但发生 memory leak
- `gdb` 不一定好定位
- `valgrind --leak-check=yes` 能较清楚报告泄漏

### 题 3：越界写 `data[100] = 0`

来源：

- 作业 4 第 5 题

结论：

- 合法下标只有 `0..99`
- `data[100]` 是越界写
- 属于未定义行为
- `valgrind` 通常报 invalid write

### 题 4：释放后访问

来源：

- 作业 4 第 6 题

结论：

- use-after-free
- 可能打印旧值，也可能崩
- `valgrind` 通常报 invalid read

### 题 5：错误的 `free`

来源：

- 作业 4 第 7 题

结论：

- 对 `data + 1` 这种非起始地址 `free`
- 属于 invalid free
- 运行库或 `valgrind` 都可能直接报错

### 题 6：`realloc` 动态向量

来源：

- 作业 4 第 8 题

考点：

- 扩容策略常用倍增
- 追加摊还复杂度接近 `O(1)`
- 连续存储，遍历局部性好
- 相比链表通常更适合“追加 + 遍历”场景

容易出错的点：

- 忘记接收 `realloc` 返回值
- 扩容失败没处理
- 继续按旧容量使用

---

## 五、实验与代码材料关联考点

虽然 `ICSTest1` 没有单独的实验说明文档，但从 `mini_tmux.cpp` 中实际使用的接口看，它和期中知识点有明显关联：

- `fork`
- `waitpid`
- `kill`
- `signal`
- `socket`
- `bind`
- `listen`
- `accept`
- `connect`
- `poll`
- `openpty`
- `ioctl`
- `dup2`
- `tcgetpgrp`

这说明老师如果把实验背景带进考试，最可能考的是：

- 多进程创建与回收
- 信号与进程组
- 前台/后台进程控制
- 阻塞 I/O 与事件轮询
- 终端、伪终端与 shell 进程关系

因此可以把实验当作“概念落地场景”来复习：

- 为什么 shell 需要 `fork + exec`
- 为什么作业控制离不开 process group 和 signal
- 为什么子进程退出后必须 `waitpid`

---

## 六、最后的冲刺建议

如果时间很紧，优先级建议如下：

### 第一优先级：一定要会

- 四类异常的区分
- `fork` 输出题
- `waitpid` 语义与僵尸进程
- 信号的 pending/block/handler
- FCFS / RR / STCF / MLFQ 的指标计算
- page fault、页表、TLB、cache 联合题
- 作业 4 的 5 类内存错误

### 第二优先级：容易拿简答分

- 用户态/内核态
- 为什么要虚拟内存
- 为什么内核栈和用户栈分离
- `gdb` 和 `valgrind` 的区别
- segmentation 和 paging 的优缺点

### 第三优先级：容易出拓展题

- 多级页表层数计算
- `sigprocmask` / `sigsuspend`
- 比例分享调度
- buddy system / GC 基本思想

---

## 七、一页速记版

- interrupt 异步，trap/fault/abort 同步
- trap 常对应系统调用
- page fault 可能可修复，也可能转成 `SIGSEGV`
- `fork` 一次返回两次，父返回子 PID，子返回 0
- 多次 `fork`：进程数 `2^n`
- zombie 已退出但未回收
- `waitpid(-1, ...)` 回收任意子进程，顺序不确定
- `kill` 是发信号，不一定“杀死”
- handler 要短小，只用 async-signal-safe 函数
- 响应时间看第一次运行，周转时间看最终完成
- SJF / STCF 优化周转，RR 优化响应
- MLFQ 通过反馈“猜”任务类型
- VM = 保护 + 共享 + 简化管理 + 用 DRAM 当 cache
- 页大小 `2^p`，页内偏移就是 `p` 位
- TLB 缓存页表项，先翻译后查 cache
- 线性页表空间大，多级页表是为了解决空间问题
- 常见内存 bug：空指针、越界、UAF、invalid free、leak
- `gdb` 找崩溃点，`valgrind` 找内存错误

---

如果你还有时间，最建议的复习顺序是：

1. 先把作业 1-5 题目和答案完整过一遍
2. 再把课件中的课堂练习重新手算一遍
3. 最后只看本资料中的“速记版”和“高频考点”
